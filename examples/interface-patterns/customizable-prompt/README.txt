A simple prompt that has a message and output value that can be themed based on the context.

Client side features:
* Context-dependent templating. Allows you to re-use the table function as a control that can be used in many circumstances but styled differently in each instance.